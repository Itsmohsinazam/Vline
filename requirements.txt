# VLINE – Python dependencies
# Install with:  pip install -r requirements.txt

opencv-python>=4.8.0        # video capture + frame processing
Pillow>=10.0.0              # image conversion for Tkinter display
numpy>=1.24.0               # array operations (bundled with opencv)
ultralytics>=8.1.0          # YOLOv11 plate detection
easyocr>=1.7.0              # OCR text extraction from plates
huggingface_hub>=0.20.0     # model weight downloads
sv_ttk>=2.5                 # Sun Valley dark theme for Tkinter
torch>=2.0.0                # PyTorch (backend for YOLO + EasyOCR)
torchvision>=0.15.0         # vision transforms
